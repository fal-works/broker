class Main {
	static function main() {
		#if !heaps
		throw "This sample requires heaps.";
		#end

		// new entities.Main();
		// new gamepad.Main();
		new collision.Main();
		// new full.Main();
	}
}
